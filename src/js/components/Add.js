const React = require('react');
const Form = require('./Form');

class Add extends React.Component {
  render() {
    return (
      <div>
        <Form />
      </div>
    );
  }
}

module.exports = Add;